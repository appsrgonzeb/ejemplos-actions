name: Multiples jobs

on:
  workflow_dispatch:

jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - name: Imprime nombre job
        run: echo Job 1 (Ubuntu)
  job-2:
    runs-on: macos-latest
    steps:
      - name: Imprime nombre job
        run: echo Job 2 (MacOs)
  job-3:
    runs-on: windows-latest
    steps:
      - name: Impreme nombre job
        run: echo Job 3 (Windows)
        
        
  
