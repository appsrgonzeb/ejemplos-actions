name: hola mundo

on: 
  schedule:
    - cron: "*/5 * * * *"
  push:
    branches:
      - main

jobs:
  saludar:
    runs-on: ubuntu-latest
    steps:
      - name: Muestra mensaje hola mundo
        run: echo "hola mundo, Felipe"
